# Agent Instructions

## Build and Verification Commands

- **Build**: `npx tsc --noEmit && npx vite build`
- **Type check only**: `npx tsc --noEmit`
- **Lint**: `npx eslint .`
- **Dev server**: `npm run dev`
- **Architecture check**: `npm run arch-check` (runs `node scripts/arch-check.mjs`)
